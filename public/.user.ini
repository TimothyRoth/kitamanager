; Per-image upload limit (a single image may not exceed this).
upload_max_filesize = 10M
; Total request body: must fit the sum of all images in one bulk upload.
; ~10 images of 10M; raise if larger batches are needed.
post_max_size = 110M
memory_limit = 256M
